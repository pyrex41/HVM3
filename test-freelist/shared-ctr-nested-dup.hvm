data L { #Nil #Cons{h t} }
@sum(xs acc) = ~xs !acc { #Nil: acc #Cons{h t}: @sum(t (+ h acc)) }
@loop(n acc) = ~n !acc {
  0: acc
  1+p: !&0{a b}=#Cons{7 #Nil}
       !&1{c d}=a
       @loop(p (+ acc (+ @sum(c 0) @sum(d 0))))
}
@main = @loop(1000 0)
